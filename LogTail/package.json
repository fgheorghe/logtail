{
  "name": "LogWatch",
  "productName": "LogWatch",
  "version": "1.0.0",
  "main": "gui/main.js",
  "dependencies": {
    "ssh2": "^0.6.1",
    "uuid": "^3.2.1",
    "moment": "^2.22.2",
    "ssh2-promise": "^0.0.9",
    "electron-settings": "^3.2.0"
  },
  "devDependencies": {
    "standard": "^11.0.1",
    "eslint": "^4.19.1",
    "eslint-config-standard": "^11.0.0",
    "electron": "^2.0.0",
    "electron-packager": "^8.1.0"
  },
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=./logtail.icns --prune=true --out=release-builds"
  }
}
